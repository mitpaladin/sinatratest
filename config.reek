
LongParameterList:
  max_params: 4  # If it's good enough for Sandi, it's good enough for us.

NestedIterators:
  max_allowed_nesting: 2
  ignore_iterators:
    - lambda

# FIXME: See troessner/reek#842; they don't *like* nested classes/namespaces.
#UnusedPrivateMethod:
#  enabled: false